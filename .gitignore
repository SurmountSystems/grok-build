/target
**/*.rs.bk
.DS_Store
result
/result

# Nested crate cargo targets (standalone crates like cargo-mem-guard)
**/target/

# Free-GHA just ci outer-loop marker (touched mid-recipe; never commit)
.ci-started

# Local scratch (invoice QR, bolt11 dumps — never commit)
/tmp/
/images/routstr-*.png
